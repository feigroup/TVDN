### Dependencies
### To install run:
### $ pip install -r requirements.txt

numpy 
scipy
rpy2
pprint
seaborn
easydict
tqdm
matplotlib

### For robustness consideration, we also call some functions from R language
### So you also need "signal" package in R
